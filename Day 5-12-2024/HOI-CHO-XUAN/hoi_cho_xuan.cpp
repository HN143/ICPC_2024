#include <bits/stdc++.h>
using namespace std;

int main(){
  freopen("inp.txt", "r", stdin);
  freopen("out.txt", "w", stdout);

  int n;
  cin >> n;
  vector<int> vt;
  for(auto &x: vt)
    cin >> x;
  sort(vt.rbegin(), vt.rend());
}